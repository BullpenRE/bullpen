<%= render 'nav_bar' %>
<!--Progress Meter, Step 1-->
<div class="container mb-4">
  <div class="progress bg-white shadow-sm mx-auto" style="max-width: 700px;">
    <div class="progress-bar" role="progressbar" style="width: 0%" aria-valuenow="0" aria-valuemin="0" aria-valuemax="100"></div>
  </div>
  <div class="text-secondary font-weight-bold my-1 text-center">0%</div>
</div>

<!--Content Card, Step 1-->
<div class="container">
  <div class="bp-card mb-5 mx-auto" style="max-width: 700px;">
    <%= form_for @freelancer_profile, url: wizard_path, method: :put do |f| %>
      <div class="text-center">
        <h2>Tell us about your skills.</h2>
        <p class="mb-4">Add tags to your application that represent your sector and operating knowledge.</p>
      </div>
      <div class="form-group mb-4">
        <label for="exampleFormControlMultiSelect" class="bp-input-label">Describe your real estate skills</label>
        <div class="w-100">
          <%= f.select :freelancer_real_estate_skills,
                       options_for_select(
                           RealEstateSkill.all.map{ |skill| [skill.description, skill.id] if skill.disable == false },
                           @freelancer_profile&.freelancer_real_estate_skills&.map do |skill|
                             [skill.real_estate_skill.description, skill.real_estate_skill.id]
                           end.flatten
                       ),
                       {}, multiple: true, class: 'select2', required: true %>
        </div>
      </div>
      <div class="form-group mb-5">
        <label for="exampleFormControlMultiSelect" class="bp-input-label">
          In what asset classes do you have experience?
        </label>
        <%= f.select :freelancer_asset_classes,
                     options_for_select(
                         AssetClass.all.map{ |asset| [asset.description, asset.id] if asset.disable == false },
                         @freelancer_profile&.freelancer_asset_classes&.map do |asset|
                           [asset.asset_class.description, asset.asset_class.id]
                         end.flatten),
                     {}, multiple: true, class: 'select2', required: true %>
      </div>
      <script>
        $(function() {
          $('.select2').select2({
            placeholder: 'Select all that apply',
            allowClear: true
          });
        });
      </script>
      <div class="d-flex justify-content-between">
        <button type="button" class="btn btn-link px-0 invisible">Back</button>
        <%= f.submit 'Next', class: 'btn btn-primary' %>
      </div>
    <% end %>
  </div>
</div>