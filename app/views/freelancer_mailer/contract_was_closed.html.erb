<p style="text-align:center;"><%= @contract.employer_profile.full_name %> closed the <b><%= @contract.title %></b> contract.</p>
<p style="text-align:center;">Go to <a href="<%= freelancer_jobs_url %>" target="_blank">Find Jobs</a> to look for other jobs.</p>
<%= render partial: 'email/button', locals: { button_txt: 'Find Jobs', url: freelancer_jobs_url } %>