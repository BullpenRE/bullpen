<br>
<%= render partial: 'email/content_block', locals: { content: "Hello #{@resource.email}," } %>
<%= render partial: 'email/content_block', locals: { content: "Your account has been locked due to an excessive number of unsuccessful sign in attempts." } %>
<%= render partial: 'email/content_block', locals: { content: "Click the button below to unlock your account:" } %>
<%= render partial: 'email/button', locals: { button_txt: 'Unlock My Account', url: unlock_url(@resource, unlock_token: @token)  } %>
