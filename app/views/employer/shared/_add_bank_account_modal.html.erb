<div class="modal" id="addBankAccountModal" tabindex="-1" role="dialog" aria-labelledby="modelLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title">Bank Account Addition</h1>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">×</span>
        </button>
      </div>
      <%= form_for :stripe, url: employer_create_account_path, method: :post do |f| %>
        <div class="modal-body">
          <div class="form-group">
            <label for="routing-number" class="h5">Bank Routing number</label>
            <%= f.text_field :bank_routing_number, class: 'form-control mb-1', id: 'routing-number', placeholder: 'Enter the ABA routing number' %>
          </div>
          <div class="form-group">
            <label for="account-number" class="h5">Bank Account number</label>
            <%= f.text_field :bank_account_number, class: 'form-control mb-1', id: 'account-number', placeholder: 'Enter the bank account number' %>
          </div>
        </div>
        <div class="modal-footer">
          <%= f.submit 'Add a Bank Account', type: 'submit', class: 'btn btn-outline-primary' %>
        </div>
      <% end %>
    </div>
  </div>
</div>