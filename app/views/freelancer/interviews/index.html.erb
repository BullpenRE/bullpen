<%= render partial: 'freelancer/navigation', locals: { section: 'interviews' } %>

<div class="container mb-3">
  <h1>Interviews</h1>
  <% @interviews.each do |job| %>
    <div class="bp-card mb-3">
      <div>
        <div class="d-flex flex-column flex-lg-row">
          <div class="text-light mb-1 ml-2 avatar-image" style="font-size: 113px; line-height: 0;"><i class="fas fa-user-circle"></i></div>
          <div class="ml-2">
            <div class="mr-auto"><h2 class="mb-1"><%= job&.employer_profile.user.full_name %></h2></div>
            <div class="bp-text-sm mb-1"><br>
              <%= job&.employer_profile.company_name %><br>
              <%= job&.employer_profile.user.location %><br>
              Плагин свертывания JavaScript используется для показа\скрытия контента. Кнопки или ссылки используются как триггеры, «нацеленные» на определенные элементы, которые надо скрыть\показать.
            </div>
          </div>
          <div class="d-flex flex-column flex-row my-2 my-lg-0">
            <div class="d-flex row-inline">
              <button type="button" class="btn btn-link text-secondary">Pending Request</button>
              <button class="btn btn-primary my-3 my-md-1 mr-2" type="button"  data-toggle="modal" data-target="#showJobPost">
                Accept Request
              </button>
              <button class="btn btn-outline-danger my-3 my-md-1" type="button"  data-toggle="modal" data-target="#showJobPost">
                Decline
              </button>
            </div>

          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>


